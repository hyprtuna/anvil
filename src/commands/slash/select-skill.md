---
name: select-skill
description: Run the skill-selection to route the current prompt context
argument-hint: "<prompt>"
---

Invoke `skill-selection`.

Analyzes the current prompt and project context, emits ordered list of candidate skills.

## Equivalent CLI

`anvil skill select <prompt>`
