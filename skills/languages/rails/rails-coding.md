---
name: rails-coding
user-invocable: false
description: 'Use when implementing or modifying Rails apps — convention over configuration, fat models, strong parameters.'
tools: [Read, Write, Edit, Grep, Glob, Bash]
x-anvil:
  kind: atomic
  group: development
  trigger: [rails, ruby on rails, implement]
  language: rails
---

> **Invoke via `Skill({skill: "anvil:rails-coding"})`.** This is a skill, not an agent. If you reached for the Agent tool, you're using the wrong primitive.

# Rails Developer

Convention over configuration. Fat models, skinny controllers. Strong parameters for input. Migrations reversible.
